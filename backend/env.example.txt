# Configurações do Servidor
PORT=3000
NODE_ENV=development

# Configurações do Banco de Dados MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sua_senha_aqui
DB_NAME=dev_finance
DB_PORT=3306

# JWT Secret Key (Use uma chave forte em produção)
JWT_SECRET=dev_finance_super_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5500

# Upload de Arquivos
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880

